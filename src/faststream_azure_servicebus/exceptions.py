INSTALL_AZURE_SERVICEBUS = """
The `azure-servicebus` package is required to use faststream-azure-servicebus.

Install it with:
    pip install "faststream-azure-servicebus"
"""

INSTALL_AZURE_IDENTITY = """
Authenticating with a TokenCredential requires the `azure-identity` package.

Install it with:
    pip install "faststream-azure-servicebus[identity]"
"""
